0) Go to the directory you want to install application (/directory/to/poker)
1) git clone git@gitlab.com:alexeydemin/poker.git
2) cd poker
3) composer install
4) Set your server Document root directory to /directory/to/poker/public
5) Open http://localhost/poker in your browser
